import { Transition } from 'react-transition-group';

export { Transition, SwitchTransition, TransitionGroup } from 'react-transition-group';
export { default as CSSTransition } from './CSSTransition';
export default Transition;
